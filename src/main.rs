// gtk / canvas
// slowly get faster?

extern crate gtk;
extern crate gdk;
extern crate gdk_pixbuf;

fn main() {
    println!("Hello, world!");


    // let tick = move || {
    //     gtk::Continue(true)
    // };
    // gtk::idle_add(tick);
    gtk::main();
}
